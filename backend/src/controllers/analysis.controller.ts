import { Request, Response } from 'express';

export const analyzeCV = (req: Request, res: Response) => {
  return res.status(200).json({ message: "Not implemented yet" });
};

export const generateLetter = (req: Request, res: Response) => {
  return res.status(200).json({ message: "Not implemented yet" });
};
